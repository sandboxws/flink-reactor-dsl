{
  "name": "flink-reactor",
  "version": "0.1.0",
  "description": "React-style TSX DSL that synthesizes to Flink SQL + Kubernetes FlinkDeployment CRDs",
  "type": "module",
  "bin": {
    "flink-reactor": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "flink",
    "stream-processing",
    "tsx",
    "dsl",
    "sql",
    "kubernetes"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "@clack/prompts": "^0.10.0",
    "picocolors": "^1.1.1",
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsup": "^8.4.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.12.0"
  }
}
